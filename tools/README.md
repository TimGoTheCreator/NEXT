# IC Generator

This folder contains the Python script `icgen.py` used to generate
initial conditions for NEXT. It outputs plain `.txt` files that NEXT
can load directly.

Supported ICs: Plummer, Hernquist, disk, composite galaxy, mergers,
uniform sphere, binary, figure-eight, cross, random solar systems.